import React from "react";

export default function GameFlashCards(props) {
  return (
    <div className="game-memory-match">
      <div className="game-left-display">
        <h1>Memory Match</h1>
        <h3>Study by playing the classic Memory Match game!  You'll get a random select of 12 cards in a 3x4 grid, from the selected deck.  Then flipping each card over, find the corresponding answer/question!</h3>
        </div>
    </div>
  );
}
